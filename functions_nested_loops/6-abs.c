#include "main.h"
/**
 * _abs - excercise five
 * @n: char
 *
 * Description: Absolute Value
 *
 * Return: does not return nothing at all
 */

int _abs(int n)
	{
		if (n < 0)
			n = n * -1;
	}
	return (n);
}
