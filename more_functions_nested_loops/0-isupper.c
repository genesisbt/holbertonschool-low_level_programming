#include 'main.h'
/**
 * main - excercise five variables
 * @c: entry one
 * Description: one to ten
 *
 * Return: does not return nothing at all
 */

int main(int c)
{
int b;

	if (c >= 'A' && c <= 'B')
{
	b = 1;
}
else
{
	b = 0;
}
return (0);
}
