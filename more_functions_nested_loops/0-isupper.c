#include "main.h"
/**
 * _isupper - excercise five variables
 * @c: entry one
 * Description: one to ten
 *
 * Return: does not return nothing at all
 */

int _isupper(int c)
{
int b;

	if (c >= 'A' && c <= 'B')
{
	b = 1;
}
else
{
	b = 0;
}
return (b);
}
