#include "main.h"
/**
 * mul - excercise five variables
 * @a: entry one
 * @b: entry two
 * Description: one to ten
 *
 * Return: does not return nothing at all
 */

int mul(int a, int b)
{
return (a * b);
}
