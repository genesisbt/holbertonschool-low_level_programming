#include "main.h"
/**
 * swap_int - excercise five variables
 * @a: entry one
 * @b: entry two
 * Description: one to ten
 *
 * Return: does not return nothing at all
 */

void swap_int(int *a, int *b)
{
	int c = *a;
	*a = *b;
	*b = c;
}
