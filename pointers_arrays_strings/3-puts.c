#include "main.h"
/**
 * _puts - excercise name
 * @str: entry one
 *
 * Description: pointers arrays strings
 *
 * Return: lenght of string
 */

void _puts(char *str)
{
	int i = 0;

	while (s[i] != '\0')
	{
		_putchar (s[i]);
		i++;
	}
	_putchar ('\n');
}
